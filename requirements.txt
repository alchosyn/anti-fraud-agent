# Runtime dependencies for npc-dialogue-ai-agent
# (Training dependencies for SFT/LoRA are in requirements-train.txt)

# LLM API client (DeepSeek via OpenAI-compatible interface)
openai>=1.0.0
python-dotenv>=1.0.0

# Web search
tavily-python>=0.3.0

# RAG: embedding + BM25 hybrid
sentence-transformers>=2.2.0
rank-bm25>=0.2.2
numpy>=1.24.0

# Benchmark metrics (evals/benchmark_public.py)
scikit-learn>=1.4
requests>=2.31.0

# Notebook utilities (for clean_notebook.py script)
nbformat>=5.0.0
